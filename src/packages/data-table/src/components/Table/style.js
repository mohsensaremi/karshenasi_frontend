import withStyles from '@material-ui/core/styles/withStyles';

export default withStyles(theme => ({
    root: {},
    tableWrapper: {},
    table: {
        textAlign: 'center',
    },
}), {flip: false});
